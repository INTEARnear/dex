[target.wasm32-unknown-unknown]
# near-sdk only uses host functions when `near` cfg is set (cargo-near sets it
# automatically, but dexes are built with plain `cargo build`)
rustflags = ["--cfg", "near"]
